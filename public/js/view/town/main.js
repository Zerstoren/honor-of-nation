define('view/town/main', [

], function (

) {

    return AbstractView.extend({
        events: {

        },
        className: 'town',

        render: function (holder, domain) {

        }
    });
});